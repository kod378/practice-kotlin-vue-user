export default {
    accessToken(state) {
        return state.accessToken;
    },
    refreshToken(state) {
        return state.refreshToken;
    },
    username(state) {
        return state.username;
    }
}